---
import "../styles/pixel-corners.css";

interface Props {
    href: string;
    name: string;
    target?: string;
}

const { href, name, target } = Astro.props;
---

<a
    class="mx-auto text-center text-text hover:text-subtle-text dark:text-dark-text dark:hover:text-dark-subtle-text transition-colors duration-200 pixel-corners"
    href={href}
    target={target || "_blank"}
    aria-label={name}
>
    <button class="p-2 cursor-pointer">
        <slot />
    </button>
</a>
