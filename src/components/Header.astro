---
interface Props {
    subtext: string;
}

const { subtext } = Astro.props;
---

<header class="site-header">
    <div class="site-title">
        <h1>RILEY DRAWARD</h1>
        <p>{subtext}</p>
    </div>
    <div class="site-actions">
        <slot />
    </div>
</header>

<style>
    .site-header {
        --site-panel-light: rgba(244, 239, 226, 0.88);
        --site-panel-dark: rgba(8, 13, 21, 0.9);
        position: relative;
        z-index: 2;
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 1rem;
        min-height: 5.5rem;
        padding: 0.8rem 6.25rem 0.8rem 1rem;
        border-bottom: 4px solid var(--dinosaur-color);
        background: var(--site-panel-light);
        box-shadow: 0 8px 0 rgba(210, 4, 45, 0.12);
    }

    :global(.dark) .site-header {
        border-bottom-color: var(--space-invader-color);
        background: var(--site-panel-dark);
        box-shadow: 0 8px 0 rgba(163, 230, 53, 0.1);
    }

    .site-title {
        min-width: 0;
    }

    .site-title h1 {
        margin: 0;
        font-size: clamp(1.8rem, 5vw, 3.6rem);
        line-height: 0.95;
    }

    .site-title p {
        margin-top: 0.45rem;
        font-size: clamp(0.72rem, 2.1vw, 1.25rem);
        line-height: 1.2;
    }

    /* Page-specific icons/buttons are slotted in here. The fixed height and
       top offset mirror the theme switcher so slotted controls line up with
       it vertically. */
    .site-actions {
        --header-control-height: 2.875rem;
        display: flex;
        align-items: center;
        gap: 0.75rem;
        align-self: flex-start;
        flex-shrink: 0;
        height: var(--header-control-height);
        margin-top: -0.3rem;
    }

    @media (max-width: 720px) {
        .site-header {
            display: grid;
            grid-template-columns: minmax(0, 1fr);
            gap: 0.6rem;
            padding: 0.7rem 5.75rem 0.65rem 0.65rem;
        }

        .site-actions {
            justify-content: space-between;
            width: 100%;
            gap: 0.5rem;
            margin-top: -0.2rem;
        }
    }
</style>
