---
import "../styles/pixel-corners.css";
import { Image } from "astro:assets";
interface Props {
    title: string;
    company: string;
    logo: ImageMetadata;
    url: string;
    isMobile?: boolean;
}

const { title, company, logo, url, isMobile = false } = Astro.props;
---

<div class:list={["block", isMobile ? "ml-12 p-3 flex-1" : "p-2 text-center"]}>
    {
        isMobile ? (
            <a
                href={url}
                target="_blank"
                rel="noopener noreferrer"
                class="flex items-center"
            >
                <Image
                    src={logo}
                    alt={`${company} logo`}
                    class="w-8 h-8 rounded-lg mr-3 bg-neutral-100 p-1"
                />

                <div class="flex-1">
                    <h3 class="text-sm font-semibold">{title}</h3>
                    <p class="text-xs">{company}</p>
                </div>
            </a>
        ) : (
            <>
                <a
                    href={url}
                    target="_blank"
                    rel="noopener noreferrer"
                    class="pixel-corners inline-block mb-3"
                >
                    <Image
                        src={logo}
                        alt={`${company} logo`}
                        class="w-16 h-16 pixel-corners dark:bg-neutral-100"
                        loading="eager"
                    />
                </a>
                <h3 class="text-sm font-semibold mb-1">{title}</h3>
                <p class="text-xs mb-2">{company}</p>
            </>
        )
    }
</div>
