---
import "../styles/pixel-corners.css";
import { Image } from "astro:assets";
interface Props {
    title: string;
    company: string;
    logo: ImageMetadata;
    url: string;
    isMobile?: boolean;
}

const { title, company, logo, url, isMobile = false } = Astro.props;
---

<div class:list={["block", isMobile ? "ml-12 p-3 flex-1" : "p-2 text-center"]}>
    {
        isMobile ? (
            <a
                href={url}
                target="_blank"
                rel="noopener noreferrer"
                class="flex items-center"
            >
                <Image
                    src={logo}
                    alt={`${company} logo`}
                    class="w-8 h-8 rounded-lg mr-3 bg-card-logo-bg dark:bg-dark-card-logo-bg p-1"
                />

                <div class="flex-1">
                    <h3 class="text-sm font-semibold text-card-text dark:text-dark-card-text">
                        {title}
                    </h3>
                    <p class="text-xs text-subtle-text dark:text-dark-subtle-text">{company}</p>
                </div>
            </a>
        ) : (
            <>
                <a
                    href={url}
                    target="_blank"
                    rel="noopener noreferrer"
                    class="pixel-corners inline-block bg-card-bg hover:bg-card-bg-hover dark:bg-dark-card-bg dark:hover:bg-dark-card-bg-hover mb-3"
                >
                    <Image
                        src={logo}
                        alt={`${company} logo`}
                        class="w-14 h-14 mx-auto p-1"
                        loading="eager"
                    />
                </a>
                <h3 class="text-sm font-semibold text-card-text dark:text-dark-card-text mb-1">
                    {title}
                </h3>
                <p class="text-xs text-subtle-text dark:text-dark-subtle-text mb-2">{company}</p>
            </>
        )
    }
</div>
