---
interface Props {
    title: string;
    href: string;
    date: string;
}

const { title, href, date } = Astro.props;
---

<a
    class="text-neutral-100 hover:text-neutral-400 duration-200 p-3 flex justify-between items-center"
    href={href}
    target="_blank"
    aria-label={title}
>
    <div class="font-medium">{title}</div>
    <div class="text-sm text-neutral-300">{date}</div>
</a>
